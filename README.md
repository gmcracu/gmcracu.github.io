# GeoSphere v1 single-file fix

Upload only `index.html` to the root of `gmcracu.github.io`. This version embeds CSS, JS and the profile photo directly, so it cannot break because of missing folders.
